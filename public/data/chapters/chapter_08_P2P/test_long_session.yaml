# JavaScript-Only CLM - Use long_session_simulation.yaml for Python
# This CLM requires Node.js and npx tsx. Skip in Python runner.
version: "1.0"
type: VCard  # Marks as definition/reference, not executable standalone
chapter:
  id: 8.7
  title: P2P Long Session Test (JavaScript Only)
  note: "For Python, use long_session_simulation.yaml instead"
clm:
  abstract:
    concept: LongSession
    description: "Long Session Accumulation Demo (Node.js version)"
  concrete:
    manifestation: Long Session Accumulation Demo
    boundary: extrinsic
    runtime: node
    builtin: run_command
    config:
      command: "npx tsx examples/run-p2p-long-session-demo.ts"
  balanced:
    expectation: Operation success

examples:
  - name: "Run Long Session Demo"
    input: {}
